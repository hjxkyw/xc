# xtpl's own output

Test sources from xtpl (version 116) with the `.tlpp` xtpl produced for each,
copied as they are. Tests compare xc's output for the same source against
them, under a normalisation each test states: xtpl names its hidden locals
differently, and reuses them across loops where xc numbers them.
